#!/usr/bin/env bash
# post-edit.sh — Post-edit hook. No-op for this markdown-only repo.
# NOT wired in settings.json by default (no formatters to run on .md files).
# Enable and configure if a linter/formatter is added to this repo.
exit 0
