---
task_id: 2026-07-20-001
agent: codex
role: isolated-goal-fidelity-adversary
status: complete
summary: |
  PASS. The second targeted recheck found no remaining content defect in the six canonical documents: the DEV finding states and success bridge are coherent, the invented two-sample closure minima are gone while comparison and freshness logic remain, and the structured human-paste function fails closed under injected guard and source failures in both Bash and zsh. Folder routing, ACC baseline limits, incident transfer, threshold discipline, secret safety, and exact six-file scope also passed. Review-state frontmatter is the sole expected post-receipt publication edit; real AVD activation of the structured/context-pinned functions remains an explicit external behavioral residual.
key_findings:
  - finding_1: All previously failing document-content lanes are closed.
  - finding_2: Exact structured block fails closed in Bash and zsh under guard and first-source failure injection.
  - finding_3: AVD behavioral activation remains external and is not promoted by this receipt.
---

# Second targeted goal-fidelity recheck

## Verdict

**PASS — all remaining document defects are closed.**

Evidence basis: **REPO-GROUNDED + RUNTIME-VERIFIED**. The receipt re-read the repaired decision surfaces in all six canonical Markdown files, executed discriminating Bash/zsh failure injections against the exact structured function, validated relative document links, checked the exact file inventory, and ran secret-shaped-content scans.

This verdict is deliberately narrower than “ACC monitor behavior is live-proven.” The documents are publication-ready after the expected review-state frontmatter update, but the real AVD consumer has not yet behaviorally activated the context-pinned wrapper and structured join/aggregation function. That external residual is preserved, not waived.

## User-goal anchor

The recheck starts from the original goal, not from the repair shape:

1. A zero-context SRE can learn the system from first principles and follow the correct folder route.
2. The completed DEV record states what was actually proved, closes the historical monitoring findings coherently, and does not silently expand the proof ceiling.
3. The Wednesday ACC runbook and ledger fail closed, separate preparation from fresh T0/live evidence, and provide explicit success/challenge/cannot-verify criteria.
4. The package contains exactly the six requested canonical documents, remains secret-safe, and transfers the post-maintenance application incident without misattributing it to CMC.

## Isolated BRAIN SCAN

- Dangerous assumption attacked: a syntactically repaired function must also stop after a failed identity guard or failed API source.
- Opposite-conclusion prediction: if the repair were only structural, Bash or zsh would return zero or emit downstream structured evidence after an injected failure, reproducing the earlier top-level `return` false green.
- Discriminating probe: execute the exact `acc_structured_sample` definition and invocation from runbook lines 217–331 with (a) `acc_guard` forced nonzero and an `acc_oc` sentinel, then (b) `acc_guard` successful and the first `acc_oc` source forced nonzero; repeat under Bash and zsh.
- Result: both shells returned status `1` for both injections. The guard test emitted no `POST_GUARD_SENTINEL`; the source test emitted the injected source marker and `STRUCTURED_SAMPLE_FAILED: Service read`, with no `SELECTED_POD`, `ENDPOINT`, `TOTAL`, `DISTRIBUTION`, or `FRESHNESS` output.
- Belief change: the earlier fail-open hypothesis is falsified for the two named failure paths. This is local shell behavioral proof, not proof that the real AVD paste path preserves every character or that ACC APIs/permissions return the required data.

## Targeted re-test matrix

| Lane | Result | Evidence and adversarial conclusion |
|---|---|---|
| Exact six-file goal | **PASS** | Runtime inventory returned exactly six `.md` files, with precisely the six canonical names requested. No seventh Markdown file was found in the folder. |
| F-002 | **PASS** | `maintenance-july-20-records-findings.md:137-144` now closes the finding with the observed component deltas and explicitly says the controller's `4Gi` reservation was not multiplied by this change. The old “target component pending” state is absent. |
| F-003 | **PASS** | `maintenance-july-20-records-findings.md:146-153` closes the finding for the observed interval, records actual-node following, and refuses to turn sampled use into a local capacity threshold. The old T0 monitoring state is absent. |
| F-004 | **PASS** | `maintenance-july-20-records-findings.md:155-162` closes the pre-existing exception finding through LIVE-14 and prevents maintenance attribution. The old “preserve and diff at T0” state is absent. |
| DEV success bridge | **PASS** | `probes-explanation.md:197-204` now states that DEV succeeded across the captured proof layers, cites convergence plus the user's completion signal, provides a concrete falsifier, and separately preserves unproved Redis quorum, end-user transactions, and later behavior. The final ledger remains bounded at `maintenance-july-20-records-findings.md:370-379`. |
| Serving closure minimum | **PASS** | The former fixed two-post-readiness-sample minimum is absent. `argocd-replica-increase-acceptance-runbook.md:431-442` instead requires membership consistency throughout every completed post-readiness sample in the externally signed observation interval. This preserves the serving comparison invariant without inventing a local sample count. |
| Metrics closure minimum | **PASS** | The former fixed two-advancing-sample closure minimum is absent. `argocd-replica-increase-acceptance-runbook.md:397-403` makes advancing timestamps/windows and complete new-Pod coverage prerequisites for calling two observations fresh; `:508-517` explicitly distinguishes the mathematically necessary before/after pair for a delta from a closure threshold and defers duration authority to signed intent/handoff. Freshness and comparison logic therefore remain stronger than a fixed count. |
| Structured human-paste block | **PASS** | `argocd-replica-increase-acceptance-runbook.md:214-331` captures each API response before `jq`, checks every source status, validates nonempty/typed data, and runs inside a function. Exact-block parsing passed under Bash and zsh. Injected guard and source failures returned nonzero in both shells and emitted no downstream evidence. |
| Folder route | **PASS** | The six entry surfaces route readers to the explainer, Wednesday ACC runbook, and ACC evidence ledger: command guide `:11`, runbook `:13`, explainer `:14`, DEV ledger `:11`, ACC ledger `:11`, and probes explanation `:10`. Relative Markdown-link validation returned `RELATIVE_MD_LINKS=PASS`. |
| ACC Application baseline boundary | **PASS** | `maintenance-july-22-records-findings.md:108-125` says the whole-fleet preparation baseline is incomplete and requires total count, distribution, exceptions, and freshness at Wednesday T0. Lines `127-131` keep the discovered `latest` risk explicitly at repository/pipeline evidence, not ACC runtime evidence. The runbook's fleet gate at `:479-485` fails closed on incomplete or stale aggregation. |
| Incident transfer | **PASS** | DEV F-014 (`maintenance-july-20-records-findings.md:333-351`) traces missing release variables → empty tag → Helm `latest` fallback → image pull failure, while bounding causality and preserving the healthy Argo CD control plane. The explainer transfers the same mechanism and operational lesson at `argocd_replica_increase_explained.md:483-503`; the ACC ledger converts it into a pre-existing-risk T0 check without asserting an ACC incident (`:127-131`). |
| No invented thresholds | **PASS** | `argocd-openshift-command-probes.md:231,290`, `probes-explanation.md:172-185`, and the runbook `:464-466,508-517` distinguish observer cadence and observed percentages from authorized success thresholds. Numeric historical observations remain evidence; no local percentage, timer, or fixed sample count authorizes closure. |
| Secret safety | **PASS** | Runtime scans across all six files found `JWT_LIKE_FILES=0` and `SECRET_ASSIGNMENT_LIKE_FILES=0`. The ACC ledger also states that the human-used token is not copied (`maintenance-july-22-records-findings.md:61`). |
| Regression/overclaim scan | **PASS** | The command guide preserves ACC full-fleet and structured-serving limitations (`argocd-openshift-command-probes.md:251-275`). The runbook preserves its proof ceiling (`argocd-replica-increase-acceptance-runbook.md:580-587`), including no end-user transaction and still-unactivated complex functions. No repaired lane promotes structural/source proof to ACC behavioral proof. |

## Dynamic failure-test results

| Shell | Injected failure | Exit | False-green sentinel/evidence | Result |
|---|---|---:|---|---|
| Bash | `acc_guard` returns nonzero | 1 | guard sentinel absent | **PASS** |
| Bash | first `acc_oc` source returns nonzero | 1 | source marker observed; structured result absent | **PASS** |
| zsh | `acc_guard` returns nonzero | 1 | guard sentinel absent | **PASS** |
| zsh | first `acc_oc` source returns nonzero | 1 | source marker observed; structured result absent | **PASS** |

The test is discriminating against the previously plausible wrong implementation: a top-level `acc_guard || return 1` can print an error yet continue and exit zero. The repaired function changes the observable outcome under the same failure class.

## Publication frontmatter disposition

**Expected post-receipt edit; not a content defect.** The following review-state fields intentionally still await this independent receipt:

- `argocd-replica-increase-acceptance-runbook.md:6` — `review_status: awaiting-independent-challenge`;
- `argocd_replica_increase_explained.md:4-5` — draft/review awaiting independent learning challenge;
- `maintenance-july-22-records-findings.md:6` — `review_status: awaiting-independent-challenge`.

The coordinator may now update those publication-review fields. It must not erase the independent behavioral ceiling encoded by the runbook's `preparation-ready-context-wrapper-avd-proof-blocked` status or rewrite the ACC ledger as if Wednesday maintenance had already occurred.

## Explicit external residual

**AVD BEHAVIORAL ACTIVATION REMAINS UNVERIFIED.**

- `argocd-replica-increase-acceptance-runbook.md:130` keeps the context-pinned wrapper structurally verified but AVD-behavior blocked.
- `argocd-replica-increase-acceptance-runbook.md:334` and `:584-587` require a human paste or isolated-kubeconfig execution before accepting structured join/aggregation evidence.
- `maintenance-july-22-records-findings.md:145-149` records why automated typing is not equivalent to consumer activation.

Promotion path: a human pastes the exact wrapper and structured function, or runs them through an isolated kubeconfig in the real AVD; the identity guard must bind to the ACC API/namespace/ArgoCD UID, all required API calls and parsers must succeed, and the resulting serving/fleet evidence must be preserved. Until that happens, ACC fleet status and the structured serving invariant remain incomplete when those forms are required.

## Meta-falsifier

This receipt would be wrong if any retained canonical file still contained an active F-002/F-003/F-004 action, if the function text tested differed from the published lines 217–331, if a real Bash/zsh invocation could continue after a guard/source failure through an untested branch, if relative routes resolved outside the intended six-file package, or if an unscanned secret form existed outside the two tested signature classes. The strongest unresolved discriminator is the real AVD activation described above; that is why the verdict is PASS for document fidelity while behavioral activation remains explicitly external.

## Disposition

- Remaining document-content defects: **0**.
- Expected post-receipt publication edit: **review-state frontmatter only**.
- External behavioral residual: **AVD activation of context-pinned/structured functions**.
- Canonical documents edited by this adversarial recheck: **none**.

