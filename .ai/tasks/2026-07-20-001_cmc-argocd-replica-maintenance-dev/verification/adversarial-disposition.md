---
task_id: 2026-07-20-001
agent: codex
status: complete
summary: Every attack is mapped to a behavioral change or explicit residual; the independent grader's current-byte traceability condition was accepted and cleared, while real AVD, Wednesday, and human-reader activation remain bounded.
---

# Adversarial disposition

## Goal-fidelity receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| GF-01 target/component unknown | Accept | Added change-intent contract, conditional status, component matrix, and no success verdict while target is unknown. |
| GF-02 hidden zero-context prerequisites | Accept | Added `Before the first probe` access/shell/auth/Lens/session-expiry contract to the explanation spec. |
| GF-03 weak `PROVEN` state | Accept | Replaced it with four cumulative proof states and wrong-target/empty-field controls. |
| GF-04 current nodes are not capacity path | Accept | Split utilization/reservation/eligibility and require scheduler constraints plus dynamic placement following. |
| GF-05 image secret blind spot | Accept | Prefer sanitized text; require visual review for every retained screenshot; prohibit secret/raw kubeconfig surfaces and repository retention of the supplied token screenshot. Residual: existing safe screenshots remain task-local but cannot be sole proof. |
| GF-06 start boundary not mechanical | Accept | Every command receives a phase class; repeated/watch/poll commands are prohibited before recorded user start. |
| GF-07 recognition-only Feynman test | Accept | Added answer-hidden novel false-green scenario and causal next-probe grading. |
| GF-08 weak reconstruction fields | Accept | Added timezone, first/last observed, capture ID, probe ID, evidence pointer, and recorded start signal. |

## SRE receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| A1 inferred target/attribution | Accept | Change-intent contract; unknown deltas remain `UNATTRIBUTED`. |
| A2 snapshot misses delayed failure | Accept; superseded | Added T0, bounded fast/slow sampling, advancing evidence, and a late checkpoint or named human handoff. Removed the unsourced two-sample/five-minute closure rule: the signed maintenance intent must supply duration; otherwise the record ends `STABLE AS OBSERVED — DURATION CONTRACT NOT SUPPLIED`. |
| A3 Ready is not service effectiveness | Accept | Added endpoints/component error/reconciliation/application freshness invariants and failure-only drill-down. |
| A4 utilization/reservation/eligibility braid | Accept | Split into three KIV panes and require per-eligible-node evidence. |
| A5 future nodes differ | Accept | Require current mapping, eligible constraints, and dynamic new pod placement. |
| A6 requests are not usage | Accept | Added distinct reservation, limit, and measured-consumption formulas/labels. |
| A7 metrics fallback cannot prove spikes | Accept | Metrics loss makes utilization/spike `UNKNOWN/BLOCKED`; fallback proves only schedulability/hard failures. |
| A8 proof status too coarse | Accept | Four cumulative proof stages plus exact shell/client/negative control. |
| A9 client/server skew | Accept | Print client/server versions and prove every exact command in the actual maintenance shell. Residual: vendor support status remains unclaimed. |
| A10 baseline staleness | Accept | Preserve preparation baseline and capture distinct start-gated T0. |
| A11 stale/sampled application status | Accept | Require full-vs-sampled scope, failure-set delta, and freshness boundary. Residual: application status does not prove end-user functionality. |
| A12 Lens not independent truth | Accept | Lens is convenience only; require same-time server/namespace/resource parity with CLI. |
| A13 no action decision | Accept | Add continue/challenge/escalate/cannot-verify/recovered table without authorizing mutation. |
| A14 attribution contamination | Accept | Added explicit attribution states; timing alone cannot promote CMC responsibility. |

## Plan consequence

The original plan cannot proceed unchanged. Major probe execution resumes only with the revised proof states, scheduler-aware capacity model, target uncertainty labels, mechanical start gate, and temporal stabilization model above. Preparation can establish a one-shot component baseline and Lens visibility; during/after claims remain unverified until the user starts maintenance.

## ACC runbook attack receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| F-01 mid-block kubeconfig drift | Accept | Pin every command to captured `ACC_CONTEXT`; reassert API per sample; discard failed/mixed samples. Residual: the wrapper must still be AVD-executed before monitor-ready promotion. |
| F-02 live delta used as intent | Accept | Independent signed intent row required for intended-completion verdict; observed delta cannot populate authorization. |
| F-03 restart reset on Pod replacement | Accept | Preserve UID, creation time, container restart/last-state, owner/revision, and node in an append-only Pod history. |
| F-04 cached green after controller recreation | Accept | Treat controller UID change as transition; require post-change `reconciledAt` or label reconciliation freshness unverified. |
| F-05 partial Redis HA | Accept | Add topology invariant across CR, old workload, HAProxy, Redis/Sentinel StatefulSet, Services, EndpointSlices, and events. Redis quorum remains explicitly unverified if unavailable. |
| F-06 Ready but old revision | Accept | Require generation/observedGeneration plus Deployment updated/available and StatefulSet revision equality. |
| F-07 Ready Pods missing as backends | Accept | Join ready Pod UIDs to EndpointSlice targetRef/ready conditions across two samples. |
| F-08 node averages hide placement | Accept | Per-actual-node reservation/allocatable/conditions/eligibility table; no capacity-safety conclusion from averages. |
| F-09 repeated stale metrics | Accept | Prefer Metrics API timestamp/window and expected-Pod coverage; otherwise freshness/utilization remains unverified. |
| F-10 sampled Applications | Accept | Record total count and full status distribution or declare fleet coverage incomplete. |
| F-11 minute-six regression | Accept | Minimum evidence floor plus late checkpoint or named handoff; first green interval cannot close the watch. |
| F-12 nominal cadence not executable | Accept | One-pass bounded fast/slow sample functions expose actual duration, failure, and missed-cycle states. Live cadence remains unverified until Wednesday dry-run/use. |
| F-13 attribution conflation | Accept | Separate intent source, temporal correlation, and actor/causal evidence in the ledger. |
| F-14 time-pressure usability | Accept | One-page cockpit and poisoned-scenario transfer test. Residual: user/new-SRE behavioral grading is still required for full USER-GRADED proof. |
| F-15 event absence overclaim | Accept | Say “no events returned at this timestamp”; preserve intermediate event deltas and sampling gaps. |

## Learning/architecture attack receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| Two reconciliation loops conflated | Accept | Opening separates `ArgoCD` CR/operator/control-plane loop from Git/`Application`/application-controller/workload loop. |
| Ambiguous current state | Accept | Use dated DEV-before, DEV-after, and ACC-preparation cards; every topology count has environment/time/proof ceiling. |
| Repeated decorative diagrams | Accept | Distinct environment/ownership, two-loop, serving-path, Redis-HA, and stabilization visuals plus one ASCII memory aid. |
| Glossary without transfer | Accept | Twelve answer-hidden scenario tests exercise prediction and diagnosis. |
| Pod/Service/EndpointSlice gap | Accept | Add serving-path explanation and `3 Ready Pods / 2 ready backends` failure case. |
| Redis HA mechanism gap | Accept | Explain sourced generic roles and label installed quorum/routing evidence ceiling. |
| Measured headroom language | Accept | ACC baseline now says low observed utilization, not schedulable headroom. |
| Sync/health axes too late | Accept | Put `solver` sorting rule in the opening; add status matrix and end-user-test boundary. |
| Temporal clock hidden | Accept | Stabilization state machine distinguishes first green, recovery, evidence floor, completion signal, and late check. |
| Snapshot expiry semantics | Accept | Every snapshot card carries capture time and required reprobe point; no unqualified “current.” |

## Continuation consequence

The new runbook and syllabus may now be drafted against the accepted controls. They remain `awaiting-independent-challenge` until a different operational verifier and fresh reader attack the completed artifacts.

## Completed-artifact fresh-reader receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| FR-F1 unexplained `CMC` | Accept | Defined CMC at first use as the authorized maintenance party; the local evidence does not expand the acronym, so the document does not invent one. |
| FR-F2 unexplained HPA/RBAC/OOM/SSO/UID/etcd | Accept | Expanded each term at first operational use and tied it to this maintenance rather than adding a detached glossary. |
| FR-F3 controller CPU spike causal overclaim | Accept | Reworded the spike as temporal coincidence and a reconciliation hypothesis; application reconciliation was not eliminated, so no exclusive Redis/replica cause is claimed. |
| FR-F4 no single observed DEV after-state | Accept | Added one connected DEV after-state architecture with namespace boundary, workload kinds, effective counts, Services, and Redis HA layers. |
| FR-F5 Git arrow implied a direct push path | Accept | Replaced the arrow with `read by repo server` into the Argo reconciliation path. |
| FR-F6 maintenance primitive not defined | Accept | Added the exact primitive: CMC edits the `ArgoCD` CR; the operator materializes control-plane workload/topology changes. |
| FR-F7 no navigation map | Accept | Added a course map and made the canonical package route explicit. |

## Completed-artifact goal-fidelity receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| GF2-01 competing entry points and DEV-ledger misroute | Accept | Added the same folder route panel to all six documents: learn from the explainer, execute ACC from the runbook, record Wednesday in the July 22 ledger, and treat DEV probes/ledger as reference only. |
| GF2-02 historical facts labelled `current` | Accept | Qualified state headings and tables by environment and capture timestamp; retained `current` only where it is a Kubernetes status-field or context command. |
| GF2-03 stale finding states | Accept | Marked superseded/closed DEV findings and recorded first/last observation plus closure evidence; Redis quorum remains explicitly unverified. |
| GF2-04 unsourced numeric thresholds | Accept | Removed 80%, percentage-point, two-sample, and five-minute decision gates. Numeric DEV observations remain facts; Wednesday duration must come from signed intent or explicit handoff. |
| GF2-05 incomplete ACC Application baseline | Accept | Added `APPLICATION FLEET BASELINE INCOMPLETE`; Wednesday T0 must capture total, full distribution, exceptions, and freshness before fleet-level claims. |
| GF2-06 unresolved frontmatter review states | Accept pending recheck | Final status will be reconciled only after the rechecks below pass; live AVD activation remains a separate residual. |
| GF2-07 AVD wrapper proof ceiling | Accept residual | Preserve `STRUCTURALLY VERIFIED, AVD BEHAVIORAL PROOF BLOCKED`; no `monitor-ready` promotion before the human-paste/isolated-kubeconfig proof. |

## Completed-artifact operational receipt

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| OV-01 EndpointSlice proof could not join Pod identity/readiness | Accept | Added structured Service, Pod, and EndpointSlice output including service name, endpoint address, readiness, Pod name, and Pod UID. The invariant now requires set equality between selected Ready Pod UIDs and ready EndpointSlice target UIDs. Exact AVD execution remains pending. |
| OV-02 Application fleet completeness was prose-only | Accept | Added deterministic JSON aggregation for total count, grouped sync/health distribution, every exception, and every `reconciledAt`; empty/truncated/failed output fails closed as incomplete. Exact AVD execution remains pending. |
| OV-03 context wrapper passed local falsification but not the AVD input path | Accept residual | Keep the wrapper ceiling explicit. Before Wednesday, run `acc_bind`, `acc_guard`, one pinned resource query, and a wrong-context negative control by human paste or isolated kubeconfig. |

## Post-maintenance incident transfer

The DEV `Synced Degraded` incident was not treated as noise. It is now a worked causal example in the syllabus and a specific ACC T0 guard in the runbook: green pipeline → empty tag → Helm `latest` fallback → missing registry manifest → Argo-synced but runtime-degraded Pods. This changes Wednesday behavior by separating a pre-existing application-delivery fault from replica-maintenance attribution.

## Recheck gate

Promotion of the completed document set requires three fresh receipts over the repaired files: zero-context reader transfer, user-verbatim goal fidelity, and operational command/proof sufficiency. The AVD wrapper's blocked behavioral proof is not a documentation failure, but it remains a live activation blocker for `monitor-ready`.

## First completed-artifact recheck: newly exposed defects

| Finding | Disposition | Behavioral change / residual |
|---|---|---|
| OP-R01 top-level guard and `oc | jq` pipeline could fail open | Accept | Replaced the pasted command stream with `acc_structured_sample()`. Every API source is captured and checked before parsing; guard/source/freshness errors return nonzero with a named failure in Bash and zsh. |
| OP-R02 Service selector membership was not derivable | Accept | The exact jq join now evaluates each Service selector against full Pod labels, excludes nonmatching Ready Pods, compares selected Ready Pod UIDs with ready EndpointSlice target UIDs, and emits `MATCH`/`MISMATCH`. |
| GF2-R03 stale F-002/F-003/F-004 and DEV success bridge | Accept | Closed all three historical findings with the live component/node/application evidence and replaced “not yet evidenced” with the captured DEV verdict plus explicit residuals. |
| GF2-R04 fixed two-sample closure minima | Accept | Removed fixed sample cardinalities as closure rules. Membership/metrics must remain consistent and fresh across the signed interval; a before/after pair is required only when a delta is claimed. |
| GF2-R06 publication frontmatter | Accept | After successful second receipts, the syllabus/runbook/ACC ledger now name completed independent document review without promoting live activation. |
| FR-R1 Git was drawn through the repo Service in the wrong direction | Accept | Diagram now shows controller → repo Service → repo-server request path, repo-server → Git clone/fetch, and repo-server → controller rendered-manifest return. |
| FR-R2 green pipeline overclaimed nested success | Accept | General rule now says only that the CI engine accepted the configured job result; nested shell actions and generated semantics remain separate. |
| FR-R3 universal negative about replica causation | Accept | Reworded to the evidenced generator cause plus “no observed mechanism connected it to the replica increase.” |
| FR-R4 Dex was functionally orphaned | Accept | Added explicitly documented-generic server → Dex SSO and Dex → external identity-provider edges. |

## Second targeted rechecks

| Receipt | Result | What survived the attempted break | Residual |
|---|---|---|---|
| `final-operational-recheck2.md` | PASS for documentation | Guard failure caused zero API calls; each source failure and missing freshness returned nonzero; selector membership excluded a Ready nonmatching Pod; exact UID matches and mismatches discriminated; Application exception could not hide. All shell, Feynman/render, read-only, and secret gates passed. | Real AVD/installed `oc` activation remains unverified. |
| `final-goal-fidelity-recheck2.md` | PASS | Exact six-file route, closed DEV states, no invented threshold, fail-closed block, ACC baseline ceiling, incident transfer, and secret boundary all survived. | Only post-receipt frontmatter and live activation remained; frontmatter is now updated. |
| `final-fresh-reader-recheck2.md` | PASS at source/document-transfer tier | Exact architecture arrows, counts, CMC/acronym bridges, CPU causal restraint, navigation, and `Synced Degraded` causal bounding survived closed/open-book attack. | Same agent saw predecessors; genuine new-human USER-GRADED transfer remains future evidence. |

## Lens/Freelens final result

The earlier WSL-folder catalog row repeatedly returned `Unauthorized`, but the explicit Eneco PowerShell path changed the diagnosis. `aliashelp` resolved the installed surface, `cmctoolsverify` passed, and `cmcfreelens dev` selected/verified DEV and exported the current context. Opening the newly synchronized `file=~\\.kube\\config` row loaded the live cluster view. Disposition: DEV authenticated monitoring is FACT/BEHAVIORAL-ACTIVATED; the earlier result was a stale duplicate-row false negative. No token or raw kubeconfig was displayed or copied. CLI remains authoritative. A separate local-tooling residual remains: bare `cmcfreelens` passes `-Mode` to an older bridge script that accepts only an environment, while `cmcfreelens <env>` works.

## Independent assurance-grade disposition

| Claim | Disposition | Behavioral change / residual |
|---|---|---|
| C-010 current bytes differed from the recheck-2 hash snapshot | **Accept — closed** | Re-ran all six Feynman/render validators, all 27 Bash fences in Bash and zsh, ShellCheck, link and secret checks, and the preserved exact-function wrong-variant harness against the current corpus. Published the six new hashes in `verification/results.md`. Refreshed the three stale `mental_model_review` pointers to `final-fresh-reader-recheck2.md`. |
| C-011 genuine human and live-environment success is future evidence | **Accept residual — narrowed** | DEV Freelens activation is now proven. ACC context-wrapper/authentication/current-row activation, signed CMC intent, Wednesday T0/live results, Redis functional quorum, end-user transactions, and genuine new-SRE grading remain named promotion gates. |
| Delivery gate: pending assurance receipt and empty witness manifest | **Accept — closed** | Verified the non-empty assurance receipt, cleared `pending_adversarial_dispatches`, registered technical, goal, reader, assurance, live-runtime, and current-byte witnesses in `manifest.json`, and retained the proof ceilings above. |

Final disposition: document/source/local-fixture work is complete. The ACC operational state remains **preparation-ready, not monitor-ready** until the human-owned activation steps succeed.
