---
task_id: 2026-04-26-001
agent: socrates-contrarian
status: complete
summary: Adversarial review of the rca-holistic skill at /Users/alextorresruiz/Dropbox/@GITHUB/@oss/stdlib/std/skills/11_sdlc/rca-holistic. Verdict PROCEED-WITH-CHANGES. Skill is a knowledge-first design with non-trivial discipline (freshness audit, command-rationale, externalized adversarial gate); it survives the H-META-1 self-application test on most axes. Real defects: filler "Skill Type"-equivalent missing from frontmatter compatibility section is fine, but boundary against "blameless postmortem" leaks (Q1, Q7), L11 forward-reference and operator-burn (AP9/AP15) escape validators (Q5), graceful-degradation status downgrade is documented but never wired into the validator (Q6, AP14), and the dogfood example is single-incident ENRICH-mode only — SYNTHESIZE mode is unproven (Q3).
---

# Verdict
PROCEED-WITH-CHANGES

# Top patches required

1. **Add boundary phrase against "postmortem" in `description`** (SKILL.md:14). Currently NOT-list says "blameless postmortem narratives for retrospective meetings" but the description's affirmative triggers include "Root Cause Analysis" and "incident explanation" — colloquially, "postmortem" matches both. Patch: insert "Distinct from blameless postmortems for retro meetings (different audience/voice)." into the description NOT-block, BEFORE the "PR descriptions" item, and add an explicit row in the Boundaries table (SKILL.md:464) reading "Postmortem narrative for blameless retro" → out-of-scope. Evidence: `description` lacks the word "postmortem" entirely (file:14), so a user prompting "write a postmortem" will activate without resistance.

2. **Add validator gates for AP9 (L11 forward-reference), AP14 (status laundering), and AP15 (operator-burning playbook).** None of G1-G8 catch these; they are the three highest-impact AP escapes. Concretely, extend `scripts/validate-rca-completeness.sh` with:
   - **G9 status-vs-mutation-log**: if `status: complete` AND no `## Mutation log` section with ≥1 entry referencing an external reviewer file, FAIL. (Catches AP14.)
   - **G10 destructive-command sniff**: in L11 fenced bash blocks, grep for `terraform apply`, `kubectl delete`, `az ... delete`, `git push`, `gh pr merge` not preceded within 4 lines by `# AUTHORIZATION REQUIRED` or `# WRITE ACTION`. (Catches AP15.)
   - **G11 vocabulary continuity**: extract bold/code-spanned proper nouns introduced first in `## L8` and verify each appears in earlier `## L1..L6`. (Catches AP6/AP9.)
   Document G9-G11 in SKILL.md §Operational surfaces (line 482).

3. **Wire the graceful-degradation status downgrade into the validator AND skeleton template.** `references/adversarial-dispatch.md:130-138` mandates `status: review` when adversaries are unavailable, and `assets/templates/rca-skeleton.md.tmpl:3` accepts `complete | review | draft`. But there is no automated check that an RCA claiming `status: complete` carries proof an external review ran. Patch: in `assets/templates/rca-skeleton.md.tmpl` the front-matter callout block (line 14) require an explicit `adversarial_review:` field with values `external | self-review | unavailable`, and have G9 (above) gate `status: complete` on `adversarial_review: external` plus a Mutation log entry. Without this, the graceful path is theatrical — every "complete" RCA in the wild can self-certify.

# Q1-Q9 attack notes (file:line citations)

**Q1.** Description triggers fire correctly on RCA / "holistic incident explanation" / "story-like reverse-engineering" / "teach-and-defend document" (SKILL.md:11-12). False-positive risk: SKILL.md:13 lists "Blameless postmortem narratives for retrospective meetings" in NOT-for, but the trigger phrase "Make a master-level explanation of what happened" (SKILL.md:455) overlaps heavily with how a stakeholder would request a retro postmortem. Phrasing "Reverse-engineer this incident" (line 454) also matches a retro reading. The boundary is named but not lexically aggressive — see patch 1.

**Q2.** Endgame discipline (DF0): (a) named reader = "Next-shift on-call engineer who has never investigated this system before" (SKILL.md:344). (b) wrong choice = "rearrangement of the inherited diagnosis: same claims, prettier headings, no challenge to inherited FACT classifications, commands without rationale, decorative diagrams" (lines 49). (c) right choice = the six-item list at lines 53-58 (endgame discipline through adversarial gate). All three present. Behavioral contract (line 34): reader can "replicate the troubleshooting end-to-end, name every component and its purpose, walk the evidence chain, defend the conclusions under adversarial questioning". DF0 satisfied. No patch.

**Q3.** Self-application (H-META-1). The skill mostly lives by its own rules: H-CMD-1 ✓ (every probe in `references/probe-rationale-pattern.md` shows WHY/EXPECTED/DECISION), H-CLAIM-1 ✓ (Phase 2 freshness audit is mandatory in dogfood README:22-26), H-VIS-1 ✓ (the visual catalog is itself prose-tested). DEFECT: dogfood example (`examples/01-mfrr-activation-crashloop/`) is a single incident, single author, ENRICH-mode only. README:30 admits "examples/02-... (not yet authored) would cover SYNTHESIZE mode." H-EXAMPLE-2 from skills-builder demands "Name which scenarios are already proven and which future prompt would still surprise the skill" — SYNTHESIZE mode (raw incident notes input) and single-repo trivial RCA are unproven. Patch: add a second example or label the skill `compatibility: ENRICH-mode-proven; SYNTHESIZE-mode-unproven` honestly. Currently README:29-30 acknowledges the gap but the SKILL.md frontmatter does not propagate it to activation surface.

**Q4.** Knowledge vs procedure rating: SKILL.md is mostly KNOWLEDGE — Mental Model 0/1/2/3 carry mental models with "why" reasoning (lines 60-152), heuristics carry CONDITION→ACTION→REASONING (lines 218-322). Three weakest sections (most procedural):
   - **Phase 0 Inventory** (lines 328-338): 4-step list, no "why each step changes a decision". Rewrite as a heuristic: "If you cannot list one inherited claim per inventory entry, the inventory is too coarse — the freshness audit will miss claims it should re-probe."
   - **Phase 6 Lessons** (lines 411-420): numbered "do this then that". Rewrite as: "A lesson that names this exact bug is a memory; a lesson that names the *class* is durable knowledge — write the class, not the bug."
   - **Phase 7 Reproducibility** (lines 424-433): bullets without "what fails if you skip". Rewrite as: "Sibling-doc coherence is the cheapest probe of your own staleness — if you cannot cite a date-aligned diff between the RCA and the PR description, the doc has lived a different life than its siblings."
   None individually fatal; together they pull SKILL.md from knowledge-centric toward checklist-shaped near the end. Apply skills-builder H-RETRO-1.

**Q5.** Validator escapes. Top three damaging APs no validator catches:
   - **AP9 (L11 forward-reference)**: G3 only counts `### Step N`; nothing checks that step N+1 doesn't reference an entity introduced in L1-L10 only. Patch: add G11 vocabulary-continuity check (top of file).
   - **AP14 (status-claim laundering)**: nothing prevents `status: complete` without an external review having run. Patch: G9 (top of file).
   - **AP15 (operator-burning playbook)**: G6 ensures rationale, G8 ensures freshness; neither flags a destructive command. Patch: G10 (top of file).
   These are non-mechanical only if the skill explicitly says so. Currently it doesn't — it implies validator-coverage is comprehensive (`scripts/validate-rca-completeness.sh:18` "all gate-outs"). Either add gates or add a paragraph to SKILL.md §Anti-slop disclaiming non-mechanical coverage of these three APs.

**Q6.** Adversary unavailability. `references/adversarial-dispatch.md:130-138` defines graceful degradation: status downgrade to `review`, header note "not independently challenged", explicit residual risk. SKILL.md:201 mirrors this. Defect: the validator does not enforce it. A user can hand-write `status: complete` and the gates pass. The graceful-degradation path is real *behavior* but theatrical *enforcement*. Patch 3 wires it.

**Q7.** Boundary near-miss "write me a postmortem for the retro meeting on yesterday's outage". Will activate (false-positive) — `description` triggers on "RCA", "holistic incident explanation", "reverse-engineering of a diagnosis"; the user's word "postmortem for retro meeting" syntactically does not match the NOT-list phrase "Blameless postmortem narratives for retrospective meetings" because Codex/Claude trigger-matching is lexical-fuzzy. The skill should state the wrong-skill in the description's NOT-list with the trigger word ("postmortem", "retro", "stakeholder"). See patch 1.

**Q8.** Cognitive-load assumption. SKILL.md:79 says scale levels to incident; DF2 (lines 172-189) gives required/skip rules per level. Dogfood is 873 lines because the incident was multi-repo / multi-day / multi-system (README:7-21) — for that reader (next-shift on-call inheriting full context transfer) 873 lines is appropriate; for a single-binary local-dev bug it would be malpractice. The skill makes the dependence on reader+incident-class explicit at SKILL.md:79 and DF2; *but* it does not give a length budget by reader-class. Patch (minor): add a row to DF2 reading "If 12 levels chosen and incident class is single-component bug, reconsider — you are probably writing the wrong endgame's RCA." This blocks the failure mode "cargo-cult dogfood".

**Q9.** Worst realistic outcome — ship a confidently-presented A1 FACT that's actually inherited stale-mirror INFER. The path: (i) author runs Phase 2 freshness audit but only against the claims the prior diagnosis explicitly enumerated; (ii) a load-bearing claim was *implicit* in the prior diagnosis (e.g., "the namespace is in the same RG") and was never enumerated → never re-probed → carried into L3 as A1 with the prior diagnosis as evidence (`references/claim-classification.md:120` already warns about agent-laundering but Phase 0 step 3 only requires enumerating "load-bearing claims arriving from outside this session" — author judgment); (iii) the next on-call uses L11 cold, hits the missing-from-RG case, runs commands against the wrong scope, takes wrong action. The freshness audit is only as complete as the inventory. Defense: G7 catches *unclassified* claims but not *misclassified* ones, and Phase 0 has no validator. Mitigation already partial — the externalized adversarial gate at Phase 5 is supposed to catch this (Dispatch 2 Q1 "Find any A1 FACT that should be A2", `references/adversarial-dispatch.md:38`). The path is real but bounded by adversarial review *if it actually runs* — which is the AP14/Q6 hole.

# Residual risks I cannot falsify (each with the resolving probe)

1. **The skill has not been used by a different agent on a different incident.** Self-application of skills-builder H-EXAMPLE-2 demands proof. Resolving probe: produce `examples/02-...` for SYNTHESIZE mode using a different incident class (single-repo, raw-notes input); run validators against it; verify all 8 gates pass.

2. **The 5+5 lexical discrimination test from skills-builder `references/description-engineering.md` was not exhibited in the rca-holistic package.** The description may undertrigger or overtrigger at scale. Resolving probe: run `skills-builder/scripts/discrimination-test.sh` against this skill's description with 10 sample prompts (5 should trigger, 5 should not, including "postmortem for retro meeting").

3. **No evidence the externalized adversarial dispatch (Phase 5 Dispatch 2) has ever rejected an RCA in dogfood.** If `socrates-contrarian` and `el-demoledor` always say PROCEED-AS-WRITTEN on this skill's outputs, the gate is theatrical. Resolving probe: review the adversarial artifacts generated for the dogfood example; if none exist or all say PROCEED-AS-WRITTEN, manufacture a deliberately broken RCA and verify the gate catches it.
