SCORE 70/100

## Itemized deductions

| Deduction | Severity | Location | Issue | Fix |
|---:|---|---|---|---|
| -5 | BLOCKING | `rca.md:3, 27-29, 275-281` | `status: complete` is stronger than the evidence. The leading token-orphan hypothesis is correctly labelled A2, but the 401 origin, KV→pod byte parity, live token state, org/bucket state, and other-writer blast radius remain A3 and directly select the repair branch. “Complete on the mechanism class” does not satisfy a complete root-cause/fix claim while those discriminators are open. The confidence paragraph also omits impact/execution flip conditions: an automated non-human bucket consumer, a broader writer set, dead admin credentials, or a valid/scoped pod token that still receives a direct InfluxDB 401. | Set `status: review` and call this a mechanism-class diagnosis until Steps 0–1 run, or absorb the in-AVD results, update the ledger, enumerate causal + impact + execution flip conditions, and only then restore `complete`. Normalize the companion review statuses at the same time. |
| -3 | HIGH | `rca.md:19-25, 122-124, 265-278` | Narrative certainty exceeds the ledger. Twelve observed exceptions do not prove “every” write failed. Repository code proves catch/no-rethrow in that source, but “trading is unaffected” and “only Grafana notices” are runtime/consumer inferences unless the deployed image and all downstream consumers are checked. No ledger A1 row needs wholesale demotion, but these source→runtime and sample→universal promotions must remain A2. | Say “12 observed rejected attempts in the retained window”; classify dashboard-only/trading-unaffected as A2 bounded by the consumer-inventory probe; add deployed-image/source provenance or a runtime completion probe. |
| -3 | HIGH | `rca.md:25, 74, 251`; `rca.md:279`; `how-to-fix.md:324` | Impact is called “low and bounded” and the L12 card says trading is unaffected before the RCA has ruled out non-human consumers or other writers. L1 and the runbook correctly add the qualifier, while the executive summary and one-pager drop it. This is internal and cross-document drift on severity/blast radius. | Make severity provisional: “observed impact is stale monitoring data; no trading impact observed.” Add a named probe to enumerate bucket consumers/alerts/reports before final severity. Carry that qualifier into Executive summary and L12. |
| -5 | BLOCKING | `how-to-fix.md:247-256, 260-283` versus `rca.md:179-185` | The package promises a safe delivery-repair branch, but Branch B cannot execute its handoff to §7 under `set -u`: it says to skip the KV write, while §7 unconditionally reads unset `NEW_TOKEN` and later requires unset `NEW_KV_HASH`. A valid KV token with stale Secret/pod bytes therefore reaches an abort, not the promised repair. This breaks the reader’s “repair” capability even though the RCA’s fix shape is conceptually correct. | Give Branch B its own executable rollout block, or set `NEW_KV_HASH="$KV_HASH"` and jump to a labelled rollout-only subsection that never reads `NEW_TOKEN`, never mutates KV, and preserves the same per-writer hash/effect gates. Re-run that branch from a fresh strict shell. |
| -3 | HIGH | `rca.md:29, 40, 184, 196-197, 254-255`; `explanation.md:203-212`; `how-to-fix.md:63-67, 146-170, 333-337` | Writer cardinality and credential-delivery knowledge drift across documents. RCA/L12 foreground b2b+b2c, while the runbook identifies three strike-price apps (`strikepricefn`, `-b2b`, `-b2c`) plus at least `telemetryingestionfn` and requires structural discovery. RCA/explanation still call CSI-vs-ESO unverified, while the later runbook presents `secret-provider-agg-kv` as observed. A reader can follow the stale L12 and leave a writer broken or revoke a shared old token too early. | Update RCA/explanation from the newer evidence, explicitly separate “three known strike-price deployments” from “full structurally discovered writer set,” and replace every hard-coded rollout instruction with “enumerate and verify every deployment resolving the secret.” |
| -3 | MEDIUM | `rca.md:91-107, 142-157` | Both diagrams render and have good teaching prose, but they visually promote disputed facts. L3 draws a direct InfluxDB→pod 401 although proxy/mesh origin is an explicit route-flipping alternative. L6 uses a solid CSI `SecretProviderClass` chain while its prose and E12 say CSI-vs-ESO was unverified. The diagrams therefore teach a cleaner mechanism than the evidence supports. | Add an explicit unknown proxy/mesh boundary to L3 and mark the 401-origin edge unresolved. In L6 either name the now-proven CSI object with new evidence or use a dotted “unverified secret-sync controller” node. State how L6 refines L3. |
| -2 | MEDIUM | `rca.md:78-89, 126-157`; first use of `CSI SecretProviderClass`, `External Secrets`, `cloud_RoleInstance`, `MC SP` | L1–L12 are all present, but L2/L5/L6 are below holistic depth. L2 has no source links, technology/artifact, deployment handoff, or out-of-scope row; L5 does not trace the declarative secret binding; L6 cannot name its delivery controller at the evidence state the RCA claims. Several operational terms arrive after the Context Ledger without introduction. | Expand L2 into repo/artifact→handoff→runtime ownership; add the secret reference/deployment binding to L5; make L6 the actual delivery state machine; define each late term in the Context Ledger or immediately before first use. |
| -3 | MEDIUM | `rca.md:14, 159-171, 208-244` | The RCA is not independently reproducible/defendable from cold. The primary live-evidence link is broken: five `../` components resolve to `log/.ai/...`; it needs six. L11’s three probes have rationale and decision rules—the focused validator passes—but omit the decisive byte-parity, 401-origin, auth/org/bucket, full-writer, and last-success probes. The last-success result is itself a confidence flip condition. | Fix the link to `../../../../../../.ai/...`. Add a compact ordered L11 continuation for every decisive probe, or explicitly define a package-level continuation into exact `how-to-fix.md` sections, including expected outputs and every branch. Add a concrete last-success probe. |
| -2 | MEDIUM | `rca.md:193-197`; `how-to-fix.md:235-245, 303-305` | L9 says the newly minted **write-only** token “writes … and reads it back.” A write-only token cannot perform the read. The runbook instead writes with the new token and queries with the admin token. The intended verifier is sound, but the RCA states an impossible actor/permission pairing. | State: write the uniquely tagged point with the new write-only token; read it back through the admin/read verifier; prove identity by the unique measurement/tag/timestamp. Keep Grafana corroborative only. |
| -1 | LOW | `explanation.md:3, 12`; `how-to-fix.md:3, 9`; `rca.md:3-5` | Companion frontmatter says `awaiting-independent-challenge` while each body claims multiple independent reviews and the RCA claims external review complete. Package readiness is internally inconsistent. | Normalize frontmatter to the actual reviewed state and link the final receipt set from all three documents. |

Total deductions: 30.

## What already passes

- All L1–L12 headings are present; L7 is a proper evidence table and does not become the document spine.
- The leading cause is explicitly a hypothesis/A2, not laundered into a “Root cause” heading.
- The two Mermaid diagrams parse and render; each has intro and read-the-picture prose.
- The focused claim-classification and L11 command-rationale validators pass. No L11 command lacks an explicit rationale and decision rule, though the cold-replay sequence is incomplete.
- The three documents agree on the core diagnosis class and high-level repair order: byte-check/origin-check → inspect live InfluxDB state → mint or repair delivery → propagate → restart discovered consumers → verify by effect.

## To reach 100

1. Resolve the in-AVD discriminators or downgrade the RCA/package to `status: review`; never claim causal completion on a branch-selecting A3.
2. Repair and cold-execute the runbook’s Branch B rollout-only path under `set -Eeuo pipefail`.
3. Reconcile the canonical writer inventory and secret-sync mechanism across RCA, explanation, runbook, Executive summary, L8, L9, and L12.
4. Make impact provisional until the bucket-consumer and shared-writer inventory is observed.
5. Redraw L3/L6 so unknown proxy and secret-sync boundaries remain visibly uncertain instead of becoming solid facts.
6. Correct L9’s write-only/readback actor split.
7. Strengthen L2/L5/L6 and L11, fix the evidence link, and add the last-success probe.
8. Normalize package review metadata; rerun the full holistic completeness gate, focused validators, Mermaid render, and a zero-context cold replay of every repair branch.

## Verdict

**REJECT as `status: complete`; PROVISIONALLY ACCEPT as a strong `status: review` mechanism-class RCA.** The document teaches the system well, the timeline and hypothesis discipline are strong, and the core diagnosis/fix shape is coherent. It is not yet defensible as complete because material branch-selecting evidence remains blocked, the visuals overstate two uncertain boundaries, blast-radius wording drifts, and the delivery-repair branch in the linked runbook is not executable as written.
