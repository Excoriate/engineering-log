# R2 ambiguity and decision-completeness review

## Findings

| Severity | Exact location | Ambiguity / dead end | Disambiguating fix |
|---|---|---|---|
| HIGH | §0 lines 18–37; all later command blocks | `set -Eeuo pipefail` makes an unhandled non-zero exit fail closed, but it does not tell the context-free executor which section failed or that the next action is §11. Several load-bearing commands rely only on this implicit exit. The cleanup runs, but the operational branch ends without a concrete handoff. | Set `CURRENT_SECTION` before every block and add an `ERR` trap that prints the section and failed command without secret values, then states `STOP → §11`. Keep the existing `EXIT` cleanup trap. |
| BLOCKING | §3 line 161 | The zero-row terminal is still not unique: after the 1-day query is empty, the executor may either “close as already-recovered” or escalate “not reproducible.” No predicate selects one. The suggested “§8 method” is not executable here because `WRITERS`, `INFLUXPOD`, admin auth, new pod identities, and rollout timestamps are established only in §§4–7. | Make the terminal deterministic: `zero rows after 1d → STOP; do not mutate; update Rec0BJKDCC4CT via §11 as NOT REPRODUCIBLE`. If observation-before-close is required, add a self-contained read-only loop here with its own writer discovery, exact deadline, positive scheduled-invocation witness, and `witness absent by deadline → §11`; do not jump to §8. |
| HIGH | §4a lines 165–169 | `head -1` silently selects one Secret/key tuple. The only assertion distinguishes empty from non-empty, so two or more matches have no decision branch even though the section promises STOP on ambiguity. | Collect every matching tuple into an array. Route `0 → STOP`, `1 → assign K8S_SECRET/SECRET_KEY`, `>1 → print names only and STOP → §11`. Remove `head -1`. |
| HIGH | §5a lines 188–196 | “Any pair differs” is now precise, but the extraction-failure guard is not: hashing an empty token produces a non-empty SHA-256 string, so three empty values can satisfy “all equal” and continue to §5b. Command success and source-byte non-emptiness remain conflated with hash non-emptiness. | Capture each extraction’s exit status and byte count before hashing. Require `POD_LEN>0`, `KS_LEN>0`, and `KV_LEN>0`; any failure/zero length → `STOP unresolved probe → §11`; only then apply `all equal → §5b`, `any inequality → §6B`. |
| BLOCKING | §5b lines 202–207 | HTTP statuses are nominally enumerated, but the `401` sub-classifier is not exhaustive. The command deletes the body and the prose defines “InfluxDB 401” mostly by absence of two known proxy markers. An unrecognized proxy 401 with neither marker is routed to §5c as if InfluxDB issued it. The `200` branch also offers “re-check §5a / scope quirk; escalate” without choosing the next action. | Capture status, headers, and body separately. Route exactly: `200 → STOP without mutation → §11`; `401` plus a positive InfluxDB JSON/error signature → §5c; `401` plus a recognized proxy signature → STOP 3 → §11; every other/unknown `401`, other status, or non-HTTP result → STOP → §11. Unknown must not default to InfluxDB. |
| BLOCKING | §5c lines 223–239 | The branch table requires “absent/inactive” versus “present and write-scoped,” but the script never produces that classification. `OLD_AUTH_ID` uses `head -1`, does not assert 0/1/>1 matches, and does not capture active state or bucket permissions. The prose fallback says an unmatchable ID is unknown, but no command converts that unknown into the branch decision. | Produce a redacted match table containing only ID, active status, and permissions; assert match cardinality. Route `0 confirmed matches → §6A`, `1 active write-scoped match → STOP → §11`, `1 inactive or wrong-scope match → §6A`, `>1 or token material unavailable → OLD_AUTH_ID unknown and STOP → §11`. Never mint from an unknown classification. |
| BLOCKING | §5c-pw lines 231–239 | The password branch is still not a complete executable path. It does not specify a secure command/UI handoff for `influxdb-admin-password`, exact UI navigation, a non-empty check or write proof for `NEW_TOKEN`, or failure handling for UI/token creation. More importantly, it rejoins §7 while `INFLUX_TOKEN` is still the dead admin token; §8’s bucket query and any §9 admin operation then fail. | Give exact UI steps: secure password retrieval/transfer, login URL, org and bucket checks, `Load Data → API Tokens → Generate API Token → Custom API Token`, write permission only for `aggregation`, hidden `read -rsp` into `NEW_TOKEN`, non-empty assertion, and the same write probe as §6A. Also create/capture a usable replacement admin credential into `INFLUX_TOKEN`, or define password-branch-specific §8/§9 commands that do not use the dead token. Every UI failure → STOP 1/2 → §11; only the fully populated state may rejoin §7. |
| BLOCKING | §6B lines 255–257 | Branch B remains prose, not the promised one-shot shell path. “Roll the writers,” “re-check §5a,” and “you are done” do not specify the exact commands, per-writer retry/STOP behavior, recomputation of hashes, or whether “done” means §8 or terminal close. The only executable rollout loop is §7, which first overwrites Key Vault and therefore cannot be reused as written. | Add a dedicated Branch-B loop that never changes Key Vault: capture the unchanged KV hash, serially restart every entry in `WRITERS`, wait for rollout, compute each new pod hash, permit one explicit retry, and route any mismatch to §11. After all writers match, rerun the §5b classifier; `200 → §8`, InfluxDB `401` or unknown/proxy result → STOP → §11. |
| BLOCKING | §8 lines 287–305 | “Empty = UNKNOWN, not zero” is explicit, but per-writer attribution is not proven. `NEWPOD` silently selects the first of zero/multiple matches; `SINCE` is an arbitrary 10-minute value or an unrecorded “rollout time”; `total=count()` counts rows in the `exceptions` table, not scheduled invocations; and the Influx query is bucket-global with no writer identity. Thus `total>0` plus one global point cannot establish an attributed successful write for every writer. No maximum wait routes a never-exercised writer to §11. | Record an exact rollout timestamp and exact new pod identity for each writer, requiring one match (or enumerate every replica). Query a positive invocation/success signal per new pod since that writer’s timestamp and separately require zero unauthorized events. Require a writer-identifying Influx tag/field for each point; if the schema has no such identity, use per-pod application telemetry as the attribution witness and label the bucket query corroboration only. Define `one schedule interval + fixed telemetry grace`; no witness by deadline → UNKNOWN → §11. |
| HIGH | §11 lines 323–324 | “The team’s on-call Slack channel” and “@-mention the on-call” remain unresolved soft references. The Lists record is exact, so STOP is safe, but the executor still must invent the Slack destination or person. | Make updating the exact Lists record the mandatory first terminal action. Name the Slack channel ID and on-call handle/user group if Slack is also mandatory; otherwise state that an unresolved channel is skipped and the Lists update is the complete fallback. |

## Prior ambiguity BLOCKINGs now closed

- CLOSED — two-plane secret handoff: the rewrite consistently requires one AVD WSL shell where both `az` and `oc` work (§0 lines 8, 16); variables no longer cross an undefined terminal boundary.
- CLOSED — admin credential lifetime on the admin-token path: `INFLUX_TOKEN` remains in the single shell through cleanup and is unset only by the `EXIT` trap (§0 lines 24–31, §5c line 212).
- CLOSED — new-token capture on §6A: `NEW_TOKEN` and `NEW_AUTH_ID` are captured without a placeholder, checked non-empty, then promoted through a mode-600 file (§6A lines 246–252; §7 lines 263–269).
- CLOSED — Branch-A post-update hash loop: `NEW_KV_HASH` is captured after the update; every entry in `WRITERS` is checked; one retry is bounded; a second mismatch explicitly STOPs (§7 lines 270–284).

Other requested ambiguity checks: the old vague “three hashes differ” wording is CLOSED by “any pair differs” (§5a line 196), and §8 explicitly states “empty = UNKNOWN, not zero” (line 289). These wording fixes do not close the residual §5a extraction-failure or §8 attribution findings above. The prior BLOCKINGs for §5b origin classification, §5c token-to-authorization identity, §5c-pw, §6B, and §8 per-writer evidence remain open.

## Verdict

**NOT-READY**

The rewrite is fail-closed at the shell level, but six reachable diagnostic/repair paths still require a context-free executor to invent a decision or evidence rule: §3 zero rows, unknown 401 origin, authorization identity, password recovery/rejoin, Branch B, and per-writer success attribution. Those are one-shot blockers, not editorial polish.
