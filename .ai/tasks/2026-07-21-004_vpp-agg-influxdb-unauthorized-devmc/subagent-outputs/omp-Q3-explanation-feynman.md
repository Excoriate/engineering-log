SCORE 61/100

## Itemized deductions

| Deduction | Severity | Location | Issue | Fix |
|---:|---|---|---|---|
| -12 | BLOCKER | `explanation.md:88-95, 172, 195-198, 269, 284` | The central 401-vs-403 rule is wrong for InfluxDB OSS 2.x's write endpoint. The official `POST /api/v2/write` contract says **401** can mean a missing/malformed token **or a token without sufficient permission to write to the org/bucket**; it does not list 403 for this endpoint. Therefore 401 does **not** rule out a valid-but-under-scoped token, H4 is ranked too low, evidence-ledger claim 9 is false, and self-test answer 1 teaches the wrong discriminator. | Replace the generic HTTP interpretation with the endpoint-specific contract from [InfluxDB OSS v2 Write data](https://docs.influxdata.com/influxdb/v2/api/write-data/). Treat 401 as “authentication header/token missing, invalid, or insufficiently permitted”; distinguish identity from scope only by direct-token testing plus authorization-permission inspection. Re-rank H4 accordingly. |
| -5 | HIGH | `explanation.md:16, 95, 165, 178, 191-201, 231-234` | The opening and causal prose say “InfluxDB is rejecting” before the response origin is proven. The document later admits proxy/mesh origin is unverified, but proxy is absent from the ranked hypothesis table. `UnauthorizedException` proves the client received a 401 during the write call; it does not prove which hop emitted it. This is inference laundering at the document's highest-visibility point. | Open with “the write request returned HTTP 401; the emitting hop is unverified.” Add proxy/mesh as a ranked hypothesis with a discriminator: compare direct pod-to-service response status/body/headers with the normal route. Promote “InfluxDB rejected it” only after those responses match. |
| -3 | HIGH | `explanation.md:11-12` | Both audit links are broken by one directory level. They resolve under `log/.ai/...`, not the repository's `.ai/...`; a zero-context reader cannot reach the evidence base or prior receipts. | Add one more `../` to each path, or use repository-root-stable links. Verify both targets resolve after the edit. |
| -5 | HIGH | `explanation.md:107-142, 277-280` | The two Mermaid diagrams are introduced, read in prose, and genuinely distinct: write-path topology versus credential chain. They teach. Coverage is still incomplete because both are topology/data-flow views. The document's decisive mechanism is temporal—KV update → secret reconciliation → pod restart → scheduled write → per-pod telemetry/Grafana effect—yet the coverage note excludes time. That exclusion is contradicted by the document's own “restart can reload the old token” warning. There is also no compact ASCII mental model or decision/evidence surface. The Feynman validator fails both the ASCII and Knowledge Contract floor. | Add a sequence diagram for propagation and verification ordering, then a compact ASCII decision ladder for direct-service 401 versus proxy 401, hash mismatch, permission mismatch, and missing org/bucket. Update the coverage note; do not claim the condition is static when recovery correctness depends on ordering. |
| -3 | MEDIUM | `explanation.md:85-87, 169-170, 270` | “Tokens do not expire by default” is directionally right but imprecise for self-managed OSS 2.x. OSS 2.x API authorizations have no native expiry/TTL field; the optional-expiry qualification for “Cloud and newer builds” is unsupported here and mixes products. The Key Vault secret's `Expiry=none` is separate metadata and does not establish the InfluxDB authorization lifecycle. Claim 10's “whether THIS token has an explicit expiry” probe is therefore the wrong question for OSS 2.x. | State: “InfluxDB OSS 2.x tokens have no native time-based expiration; they remain usable until deleted or made inactive, subject to owner/authorization state.” Keep Key Vault expiry as a separate delivery-secret fact. Add a product/version citation if any Cloud behavior is retained. |
| -4 | MEDIUM | `explanation.md:9-18` and narrative occurrences through `:214` | The opening promises the reader will “understand,” not an observable mastery outcome, and there is no Knowledge Contract. A1/A2/A3 codes are repeatedly embedded in teaching prose instead of being isolated in the evidence ledger. This forces a zero-context reader to decode audit metadata while learning the system. The bundled Feynman validator fails `knowledge_contract`, observable verbs, and 18 label-leak checks. | Add a contract using observable verbs: draw, trace, predict, diagnose, reject, and defend. Keep plain-language confidence in the narrative (“live telemetry showed,” “this remains inferred because…”); retain codes only in the evidence ledger. |
| -2 | MEDIUM | `explanation.md:42-60` | The primitive layer overstates the model: TSDB workloads are not inherently append-only or naturally expiring; retention is configured. Line protocol's tag set and timestamp are optional, and when timestamp is omitted InfluxDB assigns host UTC time. The current “exact wire format” wording presents both as mandatory. “Forcing this into SQL/Cosmos is slow and expensive” is an uncited universal, not a first principle. | Say InfluxDB is *optimized for* high-volume time-indexed writes and retention/aggregation, while alternatives can also store time series with different tradeoffs. Show optional tag/timestamp syntax and explain server-assigned time. |
| -2 | MEDIUM | `explanation.md:189, 195, 234` | `influx auth list` is presented as if it can always prove that the pod's exact plaintext token exists. In InfluxDB OSS 2.9+, token hashing is enabled by default and original token strings cannot be recovered from server metadata; behavior is version-sensitive. A direct write proves acceptance and write scope, while authorization listing requires separate admin access and may identify only authorization metadata. | Probe the live InfluxDB version first. Use the pod token in a direct, harmless uniquely identified write to test the actual contract; inspect authorization ID/status/permissions with admin access without assuming plaintext-token comparability. |
| -2 | MEDIUM | `explanation.md:282-288` | The self-test exercises causal reasoning and operational sequencing, but question 1 reinforces the false 401/403 rule and every scenario stays inside the exact incident. It tests reconstruction better than trivia, but not transfer to a nearby case. | Correct question 1. Add near-case tests such as: direct service returns 204 while the normal route returns 401; hashes match but permission inspection lacks `write-bucket`; the bucket name exists but has a new ID. Require the reader to choose the next probe and defend why. |
| -1 | LOW | `explanation.md:257-280` | The only official deep link covers tokens. Line protocol syntax and the endpoint-specific write error contract are both load-bearing and currently uncited—the latter is where the largest technical error survived. | Add official InfluxDB OSS v2 links for [line protocol](https://docs.influxdata.com/influxdb/v2/reference/syntax/line-protocol/) and [`POST /api/v2/write`](https://docs.influxdata.com/influxdb/v2/api/write-data/) to the evidence ledger or a Go-deeper section. |

Total deductions: **39**.

## To reach 100

1. Correct the endpoint-specific 401 semantics everywhere, re-rank mis-scope, and rebuild self-test question 1.
2. Bound the response origin in the opening; add proxy/mesh to the hypothesis table and a direct-service discriminator.
3. Fix the two broken audit links.
4. Add the missing temporal sequence and compact decision/mental-model visual; update the visual-coverage rationale.
5. Add an observable Knowledge Contract and move A1/A2/A3 codes out of the teaching narrative.
6. Separate OSS token lifecycle from Key Vault expiry; remove unsupported optional-expiry wording.
7. Make the primitive layer precise about configured retention, non-universal append behavior, and optional line-protocol elements.
8. Make token inspection version-aware and avoid assuming plaintext token recovery.
9. Add at least one genuinely adjacent transfer scenario and official links for every load-bearing vendor mechanism.
10. Re-run the Feynman validator and obtain a fresh-reader receipt that redraws the system and rebuilds the causal chain from the document alone.

## Verdict

**NOT APPROVED as a mastery-grade Feynman document.** The systems-thinking is otherwise strong: it covers the Grafana consumer, AVD/on-call operator, silent-write failure mechanism, b2b/b2c plus unknown writer blast radius, ordered rollout safety, and effect-based verification. The leading orphaned-token hypothesis is honestly hedged against byte mismatch and org/bucket loss. However, the document's central teaching claim—401 rules out insufficient scope—is contradicted by the official InfluxDB OSS v2 write contract; proxy origin is also promoted beyond the evidence. Those are publication blockers, not polish. The broken evidence links and failed Feynman anatomy gates further prevent a defensible 100/100 result.
