# Evidence-rigor findings

| Severity | Location | Problem | Why it matters | Fix |
|---|---|---|---|---|
| HIGH | `explanation.md:7-9`, `:193-200`, `:224-229` | The leading `A2` mechanism is promoted to fact outside section 3.4. The summary says InfluxDB “no longer recognises” the token and excludes code/configuration; Part 4 begins “the fault is an orphaned write token”; the self-test equates 401 with an orphaned/regenerated token. The evidence ledger instead calls this the leading `A2` hypothesis and leaves live token/org/bucket state `A3 [blocked: AVD/oc]` (`01-live-evidence.md:84-94`). | Readers can skip the discriminating in-AVD probe and act as though the internal token state was observed. It also hides untested alternatives in the blocked KV→Kubernetes→environment path. | Prefix the one-sentence answer with “Leading hypothesis (`A2`)”; change Part 4 to “If the in-AVD probe confirms the token is invalid…”; make the self-test answer “401 is consistent with a missing/malformed/unknown token; the orphaned-token explanation remains `A2` until E10 is run.” |
| HIGH | `explanation.md:69-81`; evidence ledger rows 8-9 at `:218-219` | Two load-bearing product-behaviour claims are labelled `A1` without a witnessed product source in `01-live-evidence.md`: tokens do not expire by default, and 401 versus 403 has the asserted exact meaning. The explainer also contradicts its own table: line 78 permits missing, malformed, or unknown credentials, but line 81 narrows 401 to “unknown to the current instance.” Startup validation rules out an empty option, not a malformed value, wrong secret injection, or other credential-path fault. | These claims eliminate the filer’s expiry theory and drive the server-side-reset diagnosis. Without an official-doc/server-behaviour witness, the strongest causal discriminator is being presented above its evidence tier. | Add authoritative InfluxDB 2.x documentation or a captured server/API source to the evidence ledger, then cite it. Even with that source, keep the observed 401 at `A1` and the specific “stored token orphaned/unknown” interpretation at `A2` until the in-AVD token check succeeds. |
| MEDIUM | `explanation.md:95-106` | The diagram names Kafka topic `asset-strikeprices-1`, then says all diagram claims are `A1`. That identifier does not appear anywhere in `01-live-evidence.md`. | A concrete identifier in an operational diagram looks probe-verified and may send an operator to the wrong topic. | Add the exact code/GitOps source row that witnesses the topic, or remove the topic name from the diagram and stop describing that node as `A1`. |
| MEDIUM | `explanation.md:179` | The related-ticket narrative adds details not present in the evidence ledger: Johnson deleting/recreating collections on dev-mc/acc “due to a datatype mismatch.” The ledger supports only the ticket ID, a delete/recreate-collection description, and a plausible unproven link (`01-live-evidence.md:91-94`). | Extra specifics make a merely adjacent ticket look like causal history and increase fabrication risk around the root-cause story. | Cite a witnessed ticket source for each detail, or reduce the sentence to: “The evidence ledger references `Rec0BGG7SPERE`, a separate delete/recreate-collection ticket; any connection is unproven (`A2`).” |
| MEDIUM | `explanation.md:108-112` | “Grafana … unaffected by the writer’s problem” is too broad. The evidence proves Grafana uses a separate read token, but it also says the 401 causes lost monitoring data and degraded dashboards (`01-live-evidence.md:67-82`). | It understates the user-visible monitoring impact while later sections correctly say Grafana is missing fresh points. | Say: “Grafana’s read credential is separate and is not shown to be failing; its dashboards are still affected because writer data is missing.” |
| LOW | `explanation.md:173-179` | The onset statement itself is appropriately hedged as “reportedly began ~1 month ago” inside an `A2` section. This matches the evidence ledger’s retention block and filer-only duration (`01-live-evidence.md:34-36`). The risk is that the subsequent “so what changed is on the InfluxDB side” wording sounds deductive even though the exact credential-sync path remains blocked. | A reader may treat the filer-reported onset as a witnessed temporal discriminator. | Keep “reported (`A2`; not telemetry-confirmed)” adjacent to the onset and replace “so what changed is” with “this makes an InfluxDB-side change the leading hypothesis.” |

## Top 3 must-fix

1. Stop promoting the orphaned-token mechanism from `A2` to fact in the summary, fix premise, and self-test.
2. Ground the token-expiry and 401/403 semantics with an authoritative witnessed source; do not label “stored token unknown” as `A1` before the in-AVD check.
3. Remove or source the unsupported `asset-strikeprices-1` identifier and the extra `Rec0BGG7SPERE` narrative details.

**Verdict — evidence labels honest? No.** The explicit diagnosis ledger is mostly disciplined, but load-bearing prose outside it upgrades the `A2` root mechanism and one unsourced 401 interpretation to witnessed fact.
