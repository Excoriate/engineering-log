# Completeness findings

| Severity | Location | Gap | Why | Add |
|---|---|---|---|---|
| **BLOCKING** | Part 3.3–3.4, before declaring the “real mechanism”; Part 4 step 1 | The explainer collapses every 401 into “the token no longer exists in InfluxDB,” even though its own authentication table says 401 also covers a **malformed** token. The evidence proves only that the Key Vault secret is enabled, non-empty, and unchanged; it explicitly leaves the KV → Kubernetes Secret → pod environment sync mechanism unverified. Startup validation proves only “non-empty,” not byte-for-byte correctness. Whitespace, newline, encoding, stale Kubernetes Secret, or wrong-secret injection therefore remain open. | Rotating an InfluxDB token before checking the credential-delivery chain can fail to repair the incident or conceal the actual sync fault. The current wording upgrades an A2 leading hypothesis to a one-sentence certainty. | Add a pre-mutation discriminator: compare **length plus a one-way hash** of the value at Key Vault, Kubernetes Secret, and the running pod without printing the secret; inspect trailing newline/encoding; identify the SecretProviderClass/ESO/other sync mechanism; and test the exact pod-supplied token against InfluxDB. Branch explicitly: byte mismatch → repair secret delivery; byte parity plus unknown token → mint a token; known token without access → inspect scope/org/bucket. Keep the headline diagnosis labelled **A2 leading** until this passes. |
| **HIGH** | Part 3.4, immediately around “reportedly began ~1 month ago” | The onset uncertainty in evidence E3 is missing. Live telemetry proves only 2026-07-21; a 2026-07-07 screenshot is no longer queryable; “>1 month” is a filer statement, not A1. | “The static credential started failing recently, therefore the server side changed” is a load-bearing causal premise. If prior successful writes with this exact credential are not established, invalid-from-first-use, stale secret injection, or an earlier configuration change remain possible. The speculative tie to `Rec0BGG7SPERE` also becomes weaker. | Add a timeline confidence box: **A1** current failures on 2026-07-21; **A2** 2026-07-07 screenshot/filer duration; **A3** exact first failure and last successful write. State how to resolve it: query any longer-retention Log Analytics/OpenShift source, inspect Grafana’s last fresh point per writer/variant, and ask for the earliest screenshot/thread. Do not use the related ticket as causal evidence unless its action time precedes the first observed failure. |
| **HIGH** | Part 2.2, Part 3.5, and Part 4 steps 1 and 4 | Blast radius is acknowledged but not operationally mapped. The document proves only `cloud_RoleName=strikepricefn`; it does not say whether that telemetry combines or distinguishes **b2b** and **b2c**, whether both variants are failing, which deployment names must be rolled, or whether telemetry/data-ingestion writers use the same KV token. The statement that Part 4 step 1 “resolves all three” is false: checking org/bucket/token state does not establish which other writers are returning 401. | A next-shift engineer can restore one deployment while leaving another variant or another writer silently dropping monitoring data. Conversely, blindly restarting every writer expands the change without proving shared exposure. | Add a blast-radius matrix with one row per known writer and variant: deployment/role, b2b or b2c, token source, latest successful write, latest 401, action required, and post-fix witness. Name telemetry and data-ingestion explicitly. Include an exact, tested App Insights query grouped by role/operation plus an in-AVD deployment/config inventory. Mark every unobserved row A3 rather than implying both strike-price variants failed. |
| **HIGH** | Part 3.5 and the start of Part 4 | The reader is told the *shape* of the first probe but not how to begin it, and the promised full how-to-fix document is not linked. Missing prerequisites include the correct AVD/CMC entry point, namespace/context confirmation, exact service/deployment names, safe secret-handling boundary, and the decision tree after each result. The document also does not enumerate what a zero-context reader still cannot do without AVD/OpenShift and Key Vault permissions. | “Port-forward and check the token” is not an executable handover for a newcomer. Under incident pressure, the missing access and identity steps are where wrong-cluster, wrong-namespace, secret disclosure, and incomplete rollout errors occur. | Add a “First 10 minutes” section or an explicit relative link to the tested runbook. It must start with access prerequisites and target confirmation, then baseline current failures, inspect service/org/bucket and credential parity, choose the matching repair branch, update KV without exposing values, roll only confirmed consumers, and collect effects. Add a separate “You still need” list for AVD access, `oc` rights, KV rights, deployment names, and the unresolved KV-to-pod sync owner/mechanism. |
| **MEDIUM** | Part 3, before the leading diagnosis | There is no ranked competing-hypothesis table. Several alternatives are only implicit: full InfluxDB outage, DNS/network failure, deleted org or bucket, malformed/wrong API token, admin-token/API-token confusion, and timestamp precision/clock errors. | Without explicit eliminators, a newcomer cannot tell “not yet checked” from “ruled out,” and the strong opening sentence encourages tunnel vision. | Add a table with hypothesis, rank, evidence, and discriminating probe. At the observed failure times, “pod fully down” and “DNS/network path broken” are low because an HTTP 401 response was received; clock/precision is low because the server rejected authentication before accepting a write; org/bucket existence and pod credential bytes remain A3; revoked/reinitialized token remains the leading A2. Do not mark an alternative rejected unless the named probe has run. |
| **MEDIUM** | Part 4 step 5 | The close criteria do not cover the scheduled path or every consumer. A manual 204 proves that one token can write, but not that KV synchronized, each pod loaded it, both b2b/b2c deployments ran, or the other writers recovered. “No new 401” can also pass before the next 15-minute trigger. | The incident can look fixed while the production code path still uses the stale token or an unobserved variant remains broken. | Require separate witnesses: manual write 204 for the candidate token; hash/length parity from KV to each pod; at least one **post-rollout scheduled invocation** per strike-price variant after the 15-minute trigger; no new 401 grouped by every affected role; and a fresh timestamp in Grafana for each affected measurement. State the observation window explicitly. |
| **MEDIUM** | Part 1.4 and Part 4 steps 1–3 | The three credentials are mentioned in different places but their non-interchangeable roles are not made explicit: writer API token, InfluxDB admin token/password, and Grafana read token. There is no warning against putting an admin credential into the application secret or rotating the Grafana token. | A zero-context responder may repair the symptom by granting the application excessive privilege or break dashboard reads while changing the wrong secret. | Add a credential-role table and a least-privilege rule: use admin credentials only to inspect/mint; write only the newly scoped writer token to `influxdb-api-token`; leave the Grafana datasource token unchanged; create the replacement before revoking anything; validate the replacement before deleting an old token. |
| **LOW** | Before Part 2 or as a short glossary | Action-critical Eneco/runtime acronyms and nouns are not defined: VPPAL, MC/dev-mc, AVD, CMC, OpenShift, `oc`, KV, RG, App Insights, and b2b/b2c. | The stated audience has zero prior context. AVD/CMC and b2b/b2c are especially important because they determine where the probe runs and how many consumers must be checked. | Add a concise glossary that defines each term in this incident’s context and points to the exact environment/namespace boundary. |

## Top 3 must-add

1. **Credential-chain discriminator before rotation:** safe Key Vault → Kubernetes Secret → pod byte-parity checks, followed by a result-driven branch. This prevents the leading “deleted token” hypothesis from being presented as proven.
2. **Executable blast-radius matrix:** b2b and b2c strike-price deployments plus telemetry/data-ingestion writers, with exact telemetry/config probes and per-consumer recovery witnesses.
3. **Evidence-bounded timeline and first-action handover:** label the onset as unconfirmed, show how to establish last success/first failure, and link or embed the tested AVD/OpenShift first-10-minutes procedure.

**Verdict:** No — it is conceptually strong, but not complete for a zero-context engineer who must safely discriminate the cause, scope every consumer, and execute the first probe.
