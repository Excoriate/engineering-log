You are 1 of 5 adversarial reviewers stress-testing a fix RUNBOOK executed ONE-SHOT by a CONTEXT-FREE agent with Eneco AVD + az access to dev-mc. The bar: ZERO ambiguity — no step where the executor must GUESS.

READ FULLY:
- RUNBOOK: log/employer/eneco/02_on_call_shift/2026_july/2026_07_21_002_johnson_vpp_agg_influxdb_unauthorized_devmc/how-to-fix.md
- context (only to judge whether an instruction is under-specified): ./rca.md, .ai/tasks/2026-07-21-004_vpp-agg-influxdb-unauthorized-devmc/context/01-live-evidence.md

YOUR LANE — AMBIGUITY / VAGUENESS. Win = find any instruction that could be executed two different ways, or where the executor cannot tell what to do next.
- Any placeholder (`<...>`) that is NOT obviously "substitute a value you discovered in step N" — i.e. an unresolved blank the executor cannot fill?
- Any command missing an EXPECTED OUTPUT or a DECISION RULE ("if X do Y")? Any branch whose outcome has no concrete next step?
- Any soft/vague word ("appropriate", "if needed", "some", "the relevant", "should") that leaves a decision undefined?
- Any expected-output described too loosely to match against real output (e.g. "identifies InfluxDB" — how exactly does the executor tell)?
- Loop/enumeration steps (`for d in … <any others>`) — is the "others" fully determined by a prior step, or a guess?

For each finding: severity, exact location, the ambiguity, the two+ ways it could be read, the concrete disambiguating fix. If a section is unambiguous, say so.

OUTPUT: write to EXACTLY .ai/tasks/2026-07-21-004_vpp-agg-influxdb-unauthorized-devmc/subagent-outputs/omp-V2-ambiguity.md ; reply with ONLY that path.
Format: findings table + Top-3 must-fix + verdict (READY-ONE-SHOT | NEEDS-CHANGES | NOT-READY). ~8 min.
